CREATE TABLE [dbo].[workstationoperation1800] (
    [id]      BIGINT        IDENTITY (5, 1) NOT NULL,
    [name]    VARCHAR (100) NOT NULL,
    [version] BIGINT        NOT NULL,
    CONSTRAINT [PK_workstationoperation1800_id] PRIMARY KEY CLUSTERED ([id] ASC)
);

